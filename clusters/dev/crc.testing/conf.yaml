cluster:
  name: crc
  domain: testing
  fqdn: crc.testing
  address: "https://kubernetes.default.svc"
  environment: dev
  datacenter: europe
  cloud: local
  region: eu
  registry: quay-dev.io

catalogSource:
  rh_catalog_version: v4.10

ingress:
  controller:
    syslog:
      address: 10.9.0.1
