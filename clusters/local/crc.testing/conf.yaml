cluster:
  name: crc
  domain: testing
  fqdn: crc.testing
  address: "https://kubernetes.default.svc"
  environment: local
  datacenter: europe
  cloud: crc
  region: eu
  registry: quay-pre.io

# catalogSource:
#   rh_catalog_version: v4.9.40
